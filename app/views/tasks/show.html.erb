<div class="container">
<p id="notice"><%= notice %></p>

<table class="table">
  <thead>
    <tr>
      <th >id</th>
      <th >name</th>
      <th >status</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><%= @task.id %></td>
      <td><%= @task.name %></td>
      <td><%= @task.status %></td>
    </tr>
  </tbody>
</table>

<br>
<div class="col-md-6"><%= link_to 'Edit', edit_task_path(@task) %></div>

<div class="col-md-6"><%= link_to 'Back', tasks_path%></div>

</div>